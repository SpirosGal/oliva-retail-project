version: '3.9'

services:
  postgres:
    image: postgres:16
    container_name: oliva_postgres
    environment:
      POSTGRES_USER: oliva
      POSTGRES_PASSWORD: oliva
      POSTGRES_DB: retail_dw
    ports:
      - "5432:5432"
      